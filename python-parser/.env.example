# Copy this file to .env and fill in real values for local development.
# In Railway, set these as environment variables on the service.

# Required: shared PostgreSQL connection (same DB as the Remix app)
DATABASE_URL=postgresql://user:password@localhost:5432/mydb

# Optional: override Tesseract binary path if not in system PATH
# TESSERACT_CMD=/usr/bin/tesseract
